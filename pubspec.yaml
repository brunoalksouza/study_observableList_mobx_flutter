name: observablelist_mobx_flutter
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0-65.0.dev <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_mobx: ^2.2.0+2
  mobx: ^2.3.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.8
  mobx_codegen: ^2.6.0+1

flutter:
  uses-material-design: true
